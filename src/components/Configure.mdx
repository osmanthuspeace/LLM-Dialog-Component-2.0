import { Meta } from '@storybook/blocks';

<Meta title="开始使用" />

# LLM 对话组件

这是一个基于 React 开发的 LLM 对话组件库，提供了一套完整的对话界面解决方案。

## 快速开始

### 安装依赖

```bash
pnpm install
```

### 启动

```bash
pnpm storybook
```
