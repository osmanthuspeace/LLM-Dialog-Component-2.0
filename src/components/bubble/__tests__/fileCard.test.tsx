import React from 'react';
import { render, screen } from '@testing-library/react';
import FileCard from '../_utils/FileCard';
import { describe, it, expect } from 'vitest';

describe('FileCard', () => {
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
  it('renders a default file');
});
